# VaultDB Community - Environment Variables
# Copy this file to .env and adjust values

# ===========================================
# Application
# ===========================================
PORT=3000
NODE_ENV=production

# Encryption key for backup files (MUST be exactly 32 characters)
ENCRYPTION_KEY=change-me-in-production-32chars!

# ===========================================
# Database (MongoDB)
# ===========================================
MONGODB_URI=mongodb://mongo:27017/vaultdb
MONGO_PORT=27017

# ===========================================
# Queue (Redis)
# ===========================================
REDIS_HOST=redis
REDIS_PORT=6379

# ===========================================
# Worker Configuration
# ===========================================
WORKER_REPLICAS=1
BACKUP_CONCURRENCY=2
RESTORE_CONCURRENCY=2

# ===========================================
# S3 Storage (Optional)
# ===========================================
# For AWS S3:
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_REGION=us-east-1
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_BUCKET_NAME=my-backups

# For MinIO (local):
S3_ENDPOINT=http://minio:9000
S3_REGION=us-east-1
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET_NAME=backups
S3_FORCE_PATH_STYLE=true

# ===========================================
# MinIO (if using --profile storage)
# ===========================================
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# ===========================================
# Test Databases (if using --profile databases)
# ===========================================
POSTGRES_PORT=5432
MYSQL_PORT=3306
